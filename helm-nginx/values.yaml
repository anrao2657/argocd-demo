replicaCount: 1

image:
  repository: nginx
  tag: latest

service:
  type: ClusterIP
  port: 80

config:
  APP_ENV: dev
  LOG_LEVEL: info

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  cpuUtilization: 70

secret:
  API_KEY: my-secret-key
